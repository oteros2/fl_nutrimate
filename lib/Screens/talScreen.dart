import 'package:flutter/material.dart';

class talScreen extends StatelessWidget {
   
  const talScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('talScreen'),
      ),
    );
  }
}